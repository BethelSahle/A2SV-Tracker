class Solution:
    def majorityElement(self, nums: List[int]) -> List[int]:
        count_item = Counter(nums)
        result = []
        for num,count in count_item.items():
            if count > len(nums) //3:
                result.append(num)

        return result
        
